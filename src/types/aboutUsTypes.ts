import { IconType } from "react-icons";

export type OurStory={
    StoryTitle:string;
    StoryDesc:string;
}

export type CoreValue={
    valueTitle:string;
    valueDesc:string;
    icon:IconType;
}